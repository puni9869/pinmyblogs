{{ define "widget/url_menu" }}
<div class="sticky top-14 z-40 flex flex-col sm:flex-row items-start sm:items-center justify-between gap-3 shadow-md bg-white rounded-l-lg rounded-r-lg rounded-b-lg border border-gray-200 px-4 py-3 mb-2 max-w-3xl">
  <!-- Left: Select all -->
  <div class="flex items-center gap-2">
    <input type="checkbox"
           id="select_all"
           class="h-4 w-4 rounded accent-indigo-600 focus:ring-2 focus:ring-indigo-500 cursor-pointer" />
    <label for="select_all"
           class="text-gray-700 font-medium select-none cursor-pointer">Select all</label>
  </div>
  <!-- Right: Actions -->
  <div class="flex items-center gap-2 ml-auto">
    <!-- bulk actions -->
  </div>
</div>
{{ end }}
